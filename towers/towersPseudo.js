function HanoiGame.prototype.run = function() {
  //until tower 0 and either tower 2 or 1 ar empty
    //ask player for a "from tower" and a "to tower"
      //if the from tower exists and has a disk
      //and the to tower exists and
        //if it has a disk, the disk is larger than the moving disk
    //pop from "from tower" and move disk to "to tower"
}
